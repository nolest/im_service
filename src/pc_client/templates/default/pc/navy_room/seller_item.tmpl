{{#each data}}
<div class="seller_list_item ds-box orient-h align-center pack-center" data-role="seller_list_item" data_seller_list_id="{{seller_user_id}}">
    <img class="ds-block inner_icon" src="{{icons}}" style="width: 40px;height: 40px;"/>
    <div class="ds-box orient-v nickname">
        <div>{{nickname}}</div>
        <div>ID:{{seller_user_id}}</div>
    </div>
    <div class="go_to_record" data-role="face-to-chat-record" data-storage="{{seller_user_id}}">录</div>
    <div class="point none"></div>
</div>
{{/each}}